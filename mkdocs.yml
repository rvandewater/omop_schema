site_name: omop_schema package
docs_dir: docs
theme:
  name: material
plugins:
  - search
  - mkdocstrings:
      handlers:
        python:
          options:
            docstring_style: sphinx
markdown_extensions:
  - admonition
  - codehilite
  - toc:
      permalink: true
